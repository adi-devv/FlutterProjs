import 'package:flutter/material.dart';

class RemindersPg extends StatelessWidget {
  const RemindersPg({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blueGrey[600],
      body: Center(
        child: Text("Reminders"),
      ),
    );
  }
}
